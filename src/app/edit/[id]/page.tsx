import React from 'react'
import CreatePage from '../../new/page'
/* import { useParams } from 'next/navigation'; */

/* type editTaskProps = {
  params: {
    id:string
  };
};
const EditTask = ({params}:editTaskProps) => {
  
  return <CreatePage params = {params} />
} */

export default CreatePage